class R1Controller < ApplicationController
	#before_action :authenticate_user!
	def index
		#@currentuser = current_user.id
		#@username = User.find(session[:user_id]).first_name
		#render plain: "login!!"
		#render body: "raw"
		#@r1 = current_user.users.first_name

	end	
end
